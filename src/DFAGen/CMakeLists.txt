SET(DFAGEN_SOURCES
    DFAGen.cc
)

ADD_EXECUTABLE(gen_dfa ${DFAGEN_SOURCES})
ADD_CUSTOM_TARGET(dfa COMMAND ${CMAKE_BINARY_DIR}/src/DFAGen/gen_dfa DEPENDS gen_dfa WORKING_DIRECTORY ${CMAKE_SOURCE_DIR})
